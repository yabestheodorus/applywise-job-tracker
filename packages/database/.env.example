# Assessment convenience: these point at an already-provisioned Supabase
# Postgres, so `cp .env.example .env` is enough to run — no DB setup or
# migrations needed. (In a real project these would be your own secrets.)

# Pooled connection (transaction mode, IPv4) — used by the app at runtime
DATABASE_URL="postgresql://postgres.lskbslvawbjkkwnealmh:hu62XXdWpWt3baj2@aws-1-ap-southeast-1.pooler.supabase.com:6543/postgres?pgbouncer=true"

# Direct connection (session mode) — only used by `prisma migrate`
DIRECT_URL="postgresql://postgres.lskbslvawbjkkwnealmh:hu62XXdWpWt3baj2@aws-1-ap-southeast-1.pooler.supabase.com:5432/postgres"
