# CLAUDE.md — Project Guide for the Claude Code Agent

This file is the **entry point**. Don't read docs from scratch — use the map below to open only the file you need for the task at hand.

## Documentation Map — read the right file, not everything

| If you need… | Read |
|---|---|
| What the product is, user flows, what's in/out of scope | [Product.md](Product.md) |
| Database models, Prisma schema, enums, multi-tenancy rules, seeded stages | [db-schema.md](db-schema.md) |
| AI/Groq behavior: extraction prompts, CV parsing, skill matching | [agents.md](agents.md) |
| Monorepo layout, tech stack, API endpoints, auth, env vars, deployment | [code-structure.md](code-structure.md) |
| Design system: colors, type, components, screens | [design.md](design.md) |
| User-facing project readme | [readme.md](readme.md) *(placeholder)* |

Rule of thumb: pick the single most relevant doc first. Only open others if that doc references them or the task genuinely spans areas.

## Keeping docs as the source of truth

These markdown files — not chat history or prior memories — are the **single source of truth**. When something changes:

1. **Make the change in the relevant doc** (schema → `db-schema.md`, AI behavior → `agents.md`, endpoints/stack → `code-structure.md`, product/scope → `Product.md`).
2. **Add a one-line entry to the Decision Log below** so the rationale is preserved.
3. Do **not** rely on remembered conversations or superseded versions — if a doc and your memory disagree, the doc wins. If the docs are silent on something, ask or decide and then record it here.

When a past decision is reversed, update the doc *and* add a new dated log entry noting what changed and why (don't silently delete the old reasoning).

## Decision Log

> Newest first. Format: `YYYY-MM-DD — <what changed> — <why> (docs touched)`

- **2026-06-20** — **Status updates store + display the formatted message.** Each `StatusEvent` now keeps the pasted update text, cleaned by the LLM, and the timeline renders it per-update. DB: `StatusEvent.rawText String?` (**migration pending**, `prisma generate` done). API: `STATUS_UPDATE_SYSTEM_PROMPT` returns a new `formattedMessage` (original message → clean Markdown: short paragraphs, `- ` bullets, optional `**bold**`; strips signatures/quoted-reply junk; same language, nothing invented); `statusSuggestionSchema` + `updateStatusSchema` gained `formattedMessage`/`rawText`; `updateStatus` persists it on the created `StatusEvent`. Web: `StatusUpdatePanel` passes the formatted text through as `rawText` (falls back to the raw paste); new dependency-free **`FormattedText`** component (renders the LLM's paragraph/bullet/bold subset — text-only, no markdown lib). Each timeline entry shows the message as a 2-line clamped **`MessagePreview`** that opens a right-side **`Sheet`** drawer (new shadcn `components/ui/sheet.tsx`) with the full formatted text + the event's stage/timestamp header. `turbo run build` green (5/5). — Updates were only a one-line note; the user wanted the full, readable message preserved per event. (db-schema.md, agents.md, code-structure.md)
- **2026-06-20** — **Board re-oriented + Upcoming rail.** Flipped the Kanban board so stages are stacked horizontal **lanes** (vertical axis) with each lane's cards laid out across the **horizontal** axis (`KanbanLane` replaces `KanbanColumn`, fixed-width cards + per-lane horizontal scroll); the board page is now a two-column pane with a compact read-only **Upcoming** rail (`UpcomingPanel`, next 8 events, `xl:`-only) on the right. — User preference. (code-structure.md)
- **2026-06-20** — **Time-flagged events + "Upcoming" agenda, end to end.** The status-update flow now captures a specific date/time (interview slot, assessment deadline, follow-up) and surfaces a cross-application agenda sorted by time. DB: new **`ScheduledEvent`** model (`userId`, `applicationId`, `title`, `type ScheduledEventType {INTERVIEW,ASSESSMENT,DEADLINE,FOLLOWUP,OTHER}`, `scheduledAt DateTime`, `note?`, `completed`) + `@@index([userId, scheduledAt])`, `Application.scheduledEvents` relation (cascade delete); `prisma generate` done — **migration not yet applied** (run `prisma migrate dev --name add_scheduled_events`). API: `STATUS_UPDATE_SYSTEM_PROMPT` now also returns an optional `event {title,type,scheduledAt}` (and gets `now` in context to resolve relative dates like "besok"); `statusSuggestionSchema`/`updateStatusSchema` extended with `event`; `updateStatus` creates the `ScheduledEvent` atomically with the stage move; `findOne` includes `scheduledEvents`. New **`events` module** — `GET /events` (Upcoming agenda, soonest first, joins application+stage), `POST/PATCH/DELETE /events` (manual add / mark done / delete). Web: new **`app/(app)/upcoming`** page (Overdue/Upcoming split, type badges, relative+absolute time, link back to app), `StatusUpdatePanel` gained an optional event sub-form (auto-filled from AI, editable, datetime-local), `ApplicationEvents` on the detail page (list + manual add + done/delete), `EventActions`/`EventTypeBadge` components, `events/actions.ts` server actions, new `lib/types.ts` event types + `formatEventWhen`/`relativeWhen` helpers, and a sidebar "Upcoming" nav item. `turbo run build` green (5/5). — User wanted interview/deadline times flagged and a "what's coming next" view. (db-schema.md, agents.md, code-structure.md, Product.md)
- **2026-06-20** — **Flow B: application detail view + AI status updates, end to end.** API: `findOne` now includes the current `status` and each `statusHistory` event's `status` relation; new `POST /applications/:id/status/extract` (Groq maps a pasted status message → an existing stage **or** a proposed new-stage label + timeline note + confidence, via `STATUS_UPDATE_SYSTEM_PROMPT`; handles Indonesian/English) and `PATCH /applications/:id/status` (resolves `statusId` or creates a stage from `newStageLabel`, then atomically updates `Application.statusId` + appends a `StatusEvent`). New DTOs: `extractStatusSchema`, `statusSuggestionSchema`, `updateStatusSchema` (refine: exactly one of `statusId`/`newStageLabel`). Web: `app/(app)/applications/[id]/page.tsx` server page (header w/ source/location/arrangement/salary/deadline/jobUrl, **Summary**, **Skill match** matched/gap/required chips, **Requirements**, **Timeline**, aside **Update status** panel); client `StatusUpdatePanel` (paste → AI suggest → editable stage Select incl. "+ Create new stage…" + note → confirm) and server `StatusTimeline`; new server actions `extractStatusUpdate`/`updateApplicationStatus`; new types `StatusEvent`/`ApplicationDetail`/`StatusSuggestion`. Board cards link to `/applications/:id` (title link + dropdown Open/Update status). Cleaned stray `console.log`s in the add dialog + `extractDraft`. `turbo run build` green (5/5). Needs `GROQ_API_KEY`. — Last undone core flow from Product.md. (Product.md, code-structure.md, agents.md)
- **2026-06-19** — **Profile + CV extraction, end to end** (TanStack Form + shared Zod). New **`@repo/api` shared contracts**: `packages/api/src/profile/profile.schema.ts` — one tolerant Zod schema (`updateProfileSchema` = `cvExtractedDraftSchema`; nullish strings, null→[] arrays) that validates the `PATCH /profile` body, the Groq CV-extraction output, AND drives the web form (Zod = Standard Schema). Trimmed `entry.ts` to export only framework-agnostic schemas (the NestJS `links` demo was dragging `@nestjs/mapped-types` into the web bundle — now unexported); moved `@repo/api` to web **dependencies**. API: `profile` module (`GET/PATCH /profile`, `POST /profile/cv`) — `cv-parser.ts` (pdf-parse v2 + mammoth), **parse-only** (5 MB limit, file not stored; persists `cvRawText`/`cvFileName`/`cvParseStatus`), extraction is synchronous and returns a review draft; `GroqService.extractJson` made generic over the schema (returns inferred output). Web: `app/(app)/profile` server page → `ProfileForm` (TanStack Form, CV upload → AI draft → editable identity/summary/links/skills/certs/languages + repeatable experiences/education via array fields → `PATCH /profile`), a reusable `TagInput`, server actions (`uploadCv`/`saveProfile`), and `apiFetch` multipart support. Needs `GROQ_API_KEY` (503 until set). `turbo run build` green. — Profile drives skill matching. (code-structure.md, agents.md)
- **2026-06-19** — **Richer application extraction.** Widened the `Application` model + new-application AI extraction beyond `{company, role, requirements, salary, deadline}`: added `location`, `workArrangement` (enum `REMOTE/HYBRID/ONSITE`), `employmentType` (enum `FULLTIME/PARTTIME/CONTRACT/INTERNSHIP`), `seniority` (free-text), `industry`, `jobUrl`, `summary`, and a **new `skills[]`** column. **Repurposed `requirements[]`** from skill tags → requirement/qualification **bullet phrases**; `skills[]` now holds the named tech and is what feeds `matchedSkills`/`gapSkills` (still computed-later, empty until a `UserProfile` exists — the add form shows them read-only as "None yet"). Touched: prisma schema (+2 enums) & `prisma generate`, API `dto.ts`/`prompts.ts`/`applications.service.ts`, web `lib/types.ts`/`actions.ts`/`add-application-dialog.tsx` (new inputs + requirements as newline-list + skills as comma-list + read-only matched/gap), and capped the paste textarea height. `turbo run build` green. **Migration not yet applied** — needs real `DATABASE_URL` + `DIRECT_URL` in `packages/database/.env`, then `prisma migrate dev`. — Extraction was too thin for a tracker. (db-schema.md, agents.md)
- **2026-06-19** — Replaced **next-themes** with a small in-house theme provider (`apps/web/components/theme/`): client `ThemeProvider`/`useTheme` (system default, localStorage key `theme`, OS-preference listener, `disableTransitionOnChange`) + a `ThemeScript` that emits the no-flash inline script via Next's `useServerInsertedHTML` (raw HTML streamed into `<head>`, never a React-reconciled `<script>` element). next-themes 0.4.6 is unmaintained and its client-rendered inline `<script>` triggers a React 19 "Encountered a script tag while rendering React component" dev-overlay error under Next 16; any `<script>` in the React tree (even from a server component) hits this, so the script must be injected outside the element tree. Updated the two consumers (`sonner.tsx`, `mode-toggle.tsx`); removed the dep. `turbo run build --filter=web` green. — Own the ~120 lines, kill the warning. (design.md)
- **2026-06-19** — First feature vertical slice — **Add application (AI) + board, end to end.** API: shared `GroqService` (`common/`, OpenAI-compatible Groq call + retry/backoff + Zod validation), `ZodValidationPipe`, `stages` module (seeds 9 default stages on first use; GET + CRUD) and `applications` module (`POST /applications/extract` → Groq draft, `POST /applications` create + initial `StatusEvent`, `GET` list/one), all scoped to `@CurrentUser('id')`. Web: authenticated **sidebar shell** under `app/(app)/` (sidebar + header + Toaster; placeholder Profile/Stages/Templates pages), server-side `apiFetch` that forwards the Supabase JWT to the API (`API_URL`), live Kanban board fetching `GET /stages`+`/applications` (replaced `lib/mock.ts` with `lib/types.ts`), and an **Add-application dialog** (paste → extract → editable AI draft → confirm) via Server Actions. Needs `GROQ_API_KEY` in `apps/api/.env` (503 until set). `turbo run build` green. — Ship the core flow. (Product.md, agents.md, code-structure.md)
- **2026-06-19** — Dev ports split: **web → 3000**, **API → 3001** (API `main.ts` reads `PORT` via `ConfigService`, default `3001`; `PORT=3001` added to `apps/api/.env`). Both defaulted to 3000 and collided; the Google/Supabase OAuth redirect is pinned to `http://localhost:3000/auth/callback`, so the web app keeps 3000. — Avoid the port clash. (code-structure.md)
- **2026-06-19** — Built **web auth** (`apps/web`) on Supabase Auth via `@supabase/ssr`: browser/server Supabase clients (`lib/supabase/`), a `proxy.ts` (Next 16 proxy convention — renamed from the deprecated `middleware.ts`) that refreshes the session and gates routes (unauth → `/login`), `login`/`signup` pages under `app/(auth)/` sharing `auth-form.tsx` (email+password via Server Actions in `actions.ts`, **Google** via client `signInWithOAuth`), an `/auth/callback` route (`exchangeCodeForSession`) and `/auth/signout` route, and a header `UserMenu` (email + sign-out). Web env (`apps/web/.env.local`): `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY`. Google requires the provider enabled in Supabase + `${SITE_URL}/auth/callback` allowed. — Real multi-user sign-in/up. (code-structure.md)
- **2026-06-19** — API config hygiene: providers/guards read env through Nest's `ConfigService` (`getOrThrow`), never `process.env`; and `PrismaService` now lives in a `@Global()` `CommonModule` (`common/`) instead of a dedicated `PrismaModule` (removed). — Idiomatic Nest DI for config + a single home for cross-cutting providers. (code-structure.md)
- **2026-06-19** — Wired the API foundation in `apps/api`: global `PrismaModule`/`PrismaService` (`PrismaService extends PrismaClient` from `@repo/database`, `@prisma/adapter-pg` on `DATABASE_URL`, connect/disconnect via lifecycle + `enableShutdownHooks`); `SupabaseAuthGuard` registered globally via `APP_GUARD` (verifies the `Bearer` JWT against the project JWKS at `SUPABASE_JWKS_URL` — asymmetric ES256/RS256 — using `jose`'s `createRemoteJWKSet`, attaches `req.user`, 401 on failure); `@Public()` opt-out decorator (used by public `GET /health`) and `@CurrentUser()` param decorator (returns `AuthUser`/field). `ConfigModule.forRoot({ isGlobal: true })` loads `.env`. Added deps `@repo/database`, `@prisma/adapter-pg`, `jose`, `@nestjs/config`; `turbo run build --filter=api` green. — Stand up auth + DB access before feature modules. (code-structure.md)
- **2026-06-19** — Implemented `packages/database` (`@repo/database`): full Prisma schema (Application, StatusStage, StatusEvent, UserProfile, WorkExperience, Education + Source/CvParseStatus enums) using Prisma 7's `prisma-client` generator (TS output to `src/generated/prisma`, gitignored). Added a `src/index.ts` entry that exports a `prisma` singleton via the `@prisma/adapter-pg` driver adapter (pooled `DATABASE_URL`; migrations use `DIRECT_URL`) and re-exports all generated types. Built to CJS `dist/` with `tsc -b` (composite) so the NestJS API consumes it as a compiled dep; full `turbo run build` green. Reconciled db-schema.md to the implemented schema (`StatusEvent` cascade+index, `WorkExperience`/`Education` indexes, `UserProfile.createdAt` and `templateAnswers` default). — Stand up the data layer. (db-schema.md)
- **2026-06-19** — Added **ApplyWise** branding: auto-cropped the supplied transparent PNGs to content; `apps/web/public/logo.png` in the header (mono-white via `dark:brightness-0 dark:invert` in dark mode), `apps/web/app/icon.png` as favicon + `apple-icon.png` (Next metadata convention, removed default `favicon.ico`). Page title → "ApplyWise". Docs reconciled from "TrackJob" → **ApplyWise** (the official project name). — Use the real brand assets.
- **2026-06-19** — Built a mock-data home page (Kanban board) to validate the design system: app header + theme toggle, stage columns, application cards with stage-color borders, status badges, urgency date chips, emerald/red skill chips, react-icons source logos. Added `next-themes`, `react-icons`, and shadcn `card/badge/dropdown-menu/avatar/separator/scroll-area/tooltip`. — Visual sign-off on the design system before building real features.
- **2026-06-19** — Fixed `packages/api` `dev` script: `pnpm build --watch` → `tsc -b -v --watch`. The `with-nestjs` example was authored for pnpm; we use **npm workspaces**, so the `pnpm` call 404'd on workspace packages and broke `turbo run dev`. (No remaining `pnpm` references.) — Make `npm run dev` work across the monorepo.
- **2026-06-19** — Installed Tailwind v4 + shadcn/ui (latest, `radix-nova`/`stone`, lucide) in `apps/web`; applied the emerald/stone theme + `0.75rem` radius in `globals.css` and wired Inter (`--font-sans`) + Cal Sans (`--font-heading`) in `layout.tsx`. Added `@/*` path alias + PostCSS config. `apps/web` builds green. — Implement the design system foundation. (design.md, code-structure.md)
- **2026-06-19** — Defined the design system: warm & friendly, light-first (both themes), **emerald** brand on **warm stone** neutrals, restrained color on the Kanban board. Type: **Cal Sans** headings + **Inter** body (Inter `tabular-nums` for numbers, no mono). Icons: **lucide-react** for UI + **react-icons** for brand/source logos (no hand-authored SVGs). Tooling: Tailwind v4 + shadcn/ui (`new-york`/`stone`) — not yet installed. — Lock the visual language before building UI. (design.md)
- **2026-06-19** — Scaffolded the monorepo from `create-turbo@latest --example with-nestjs` (npm workspaces). Example ships `packages/{api,ui,eslint-config,jest-config,typescript-config}` — kept as-is for now; `packages/database` (Prisma) is a planned addition and the example `links` demo will be replaced. `npm run build` is green. — Get a working Next.js + NestJS Turborepo baseline. (code-structure.md)
- **2026-06-19** — Set `strictPropertyInitialization: false` in `packages/typescript-config/nestjs.json`. — Idiomatic NestJS: decorator-based entities/DTOs declare properties without initializers; the rest of `strict` stays on. Without it the example failed to build (TS2564). (code-structure.md)
- **2026-06-19** — Split the monolithic `trackjob_ai_context.md` into per-topic docs (Product / db-schema / agents / code-structure) + blank readme/design, and added this CLAUDE.md router. — Keep context focused; agent reads only what a task needs. (all docs)
- **2026-06-19** — Status updates are a separate flow from creating an application: the status-update textarea lives inside the job **detail view** (`POST /applications/:id/status/extract`), not the add form; `/applications/extract` no longer does new-vs-update detection. — Clearer UX; the app is already known from the URL so the AI prompt is simpler and more accurate. (Product.md, code-structure.md, agents.md)
- **2026-06-19** — App is **multi-tenant** via Supabase Auth; every top-level model carries `userId`, the API uses a `SupabaseAuthGuard`, cross-tenant access returns `404`. No Prisma `User` model — `userId` is the Supabase UUID string. — Move from single-user MVP to real multi-user. (db-schema.md, code-structure.md, Product.md)
- **2026-06-19** — Richer `UserProfile` with **CV upload + AI extraction** (`WorkExperience`, `Education`, identity/summary/links/certifications/languages, `cvParseStatus`); CV files stored in a private Supabase Storage bucket; extraction returns a review-before-save draft. — A complete profile drives better skill matching. (db-schema.md, agents.md, code-structure.md, Product.md)
- **2026-06-19** — Work/education dates stored as **strings**, not `DateTime`. — CVs format dates inconsistently ("Mar 2021", "Present"); coercion would lose data. (db-schema.md)
